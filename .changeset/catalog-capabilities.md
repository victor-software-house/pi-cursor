---
"pi-cursor-inference": patch
---

Advertise image, thinking, context-window, and Max Mode support from each model's matched Cursor catalog metadata, and omit unmatched selections instead of assigning permissive fallback capabilities.
